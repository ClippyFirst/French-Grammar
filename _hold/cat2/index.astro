---
---
import Base from '../../../layouts/Base.astro';
export function getStaticPaths() {
  return [
    { params: { category: 'nouns' } },
    { params: { category: 'verbs' } },
  ];
}
export const prerender = true;
const c = Astro.params.category;
---
<Base>
  <Fragment slot="head">
    <title>cat {c}</title>
  </Fragment>
  <h1>Category {c}</h1>
</Base>