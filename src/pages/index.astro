---
import Base from '../layouts/Base.astro';
const x = await Promise.resolve(5);
---
<Base>
  <Fragment slot="head">
    <title>minimal {x}</title>
  </Fragment>
  <h1>HELLO {x}</h1>
  <p>minimal test</p>
</Base>